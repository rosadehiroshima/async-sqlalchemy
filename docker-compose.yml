services:
  postgres:
    image: postgres:16-alpine
    container_name: asyncpg
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pwd
      POSTGRES_DB: asyncio
    ports:
      - 5432:5432
